# nats-server config for token-auth integration tests.
# Used by docker-compose.yml nats-token-auth service.
# Value must match NATS_TEST_TOKEN env var in the compose file.
authorization {
  token: "test-token-changeme"
}

jetstream {
  store_dir: /data
}
