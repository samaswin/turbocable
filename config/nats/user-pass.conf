# nats-server config for user+password integration tests.
# Used by docker-compose.yml nats-user-pass service.
authorization {
  users: [
    { user: "testuser", password: "testpassword" }
  ]
}

jetstream {
  store_dir: /data
}
