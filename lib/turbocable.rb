# frozen_string_literal: true

require_relative "turbocable/version"

# Turbocable is a pure-Ruby publisher for the TurboCable fan-out pipeline.
#
# Phase 0 ships the skeleton only — the public surface (configuration,
# codecs, broadcast, auth, null adapter) lands in subsequent phases per
# implementation-phases.md.
module Turbocable
end
